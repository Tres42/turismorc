{% extends 'T42TestimoniosBundle::layout.html.twig' %}

{% block content %}

<h1>Editar Testimonio</h1>

<form action="{{ path('testimonio_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button class="btn btn-info" type="submit">Editar</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('testimonio') }}">
            Volver a Testimonios
        </a>
    </li>
    <li>
        <form action="{{ path('testimonio_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
                <button class="btn btn-danger" type="submit">Eliminar</button>
        </form>
    </li>
</ul>

{% endblock %}
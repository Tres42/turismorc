{% extends 'T42TestimoniosBundle::layout.html.twig' %}

{% block content %}
    <div class="row">
        <div class="span12">
            <h2>Testimonios</h2>
            <div class="row">
                <div class="span3">
                    <ul class="nav nav-tabs nav-stacked">
                        {% if is_granted('ROLE_TESTIMONIOS_ADD') %}                            
                            <li><a href="{{ path('t42_testimonios_backend_new') }}"><i class="icon-plus"></i> Agregar nuevo</a></li>
                        {% endif %}
                        <li><a href="{{ path('t42_backend_index') }}"><i class="icon-arrow-left"></i> Volver</a></li>
                    </ul>
                </div>
                <div class="span5 offset2">
                    <table class="table table-condensed table-striped">
                        <thead>
                            <tr>
                                <td>Nombre</td>
                                <td>Organizaci&oacute;n</td>
                            </tr>
                        </thead>
                        <tbody>
                        {% for entity in entities %}
                            <tr>
                                <td>{{ entity.nombre }}</td>
                                {% spaceless %}
                                <td>
                                    {% if entity.organizacion is not empty %}
                                        {{ entity.organizacion }}
                                    {% else %}
                                        <i>n/d</i>
                                    {% endif %}
                                </td>
                                {% endspaceless %}
                                <td>
                                    {% if is_granted('ROLE_TESTIMONIOS_EDIT') %}
                                        <a href="{{ path('t42_testimonios_backend_edit', { 'id': entity.id }) }}"><i class="icon-pencil"></i></a>                                        
                                    {% endif %}    
                                    {% if is_granted('ROLE_TESTIMONIOS_DELETE') %}
                                        <a href="{{ path('t42_testimonios_backend_delete', { 'id': entity.id }) }}"><i class="icon-remove"></i></a>
                                    {% endif %}    
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
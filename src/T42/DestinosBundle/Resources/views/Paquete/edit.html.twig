{% extends 'T42DestinosBundle::layout.html.twig' %}

{% block content %}

<h1>Editar Paquete de viajes</h1>

<form action="{{ path('destinos_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button class="btn btn-info" type="submit">Editar</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('destinos') }}">
            Volver a listado de paquetes
        </a>
    </li>
    <li>
        <form action="{{ path('destinos_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
                <button class="btn btn-danger" type="submit">Eliminar</button>
        </form>
    </li>
</ul>

{% endblock %}